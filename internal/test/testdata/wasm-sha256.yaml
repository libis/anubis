# Forces every client through the sha256 WASM challenge so that the browser
# tests exercise the WASM path rather than the default "fast" challenge.
bots:
  - name: everyone
    user_agent_regex: .
    action: CHALLENGE
    challenge:
      algorithm: sha256
      difficulty: 2
